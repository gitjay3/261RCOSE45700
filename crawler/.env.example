# placeholder — populated in Story 1.3 (로컬 개발 환경 구성)

# Story 2.2 — ProxyProvider (ProxyBroker 구현체).
# 미설정 시 ProxyBroker.get_proxy()는 None 을 반환하여 "프록시 없음" 모드로 동작.
# Story 2.5/2.6 진입 시 실측 기반 NodeMaven 도입 검토.
PROXY_BROKER_HOST=
PROXY_BROKER_USER=
PROXY_BROKER_PASS=
